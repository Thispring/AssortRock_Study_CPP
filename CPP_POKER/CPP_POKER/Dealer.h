// Dealer는 Player에게 카드를 무작위로 나누어 주는 역할을 합니다.

#pragma once

// 사용자 정의 헤더
#include "GameManager.h"

class Dealer {

public:
	// 무작위로 카드 숫자를 반환하는 함수(1 ~ 13)
	//int RandomNumber();
	// 무작위로 카드 문양 번호를 반환하는 함수(1 ~ 4)
	//int RandomShape();

	// Player가 되돌리기를 했을 경우, 52장의 카드에서 전달해준 카드와
	// 전달 받은 카드를 제외하고 무작위로 카드를 전달하는 함수
};
